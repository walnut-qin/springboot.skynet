<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kaos.his.mapper.common.config.ConfigVariableMapper">
    <resultMap id="map" type="com.kaos.his.entity.common.config.ConfigVariable">
        <id column="NAME" jdbcType="VARCHAR" property="name"/>
        <result column="VALUE" jdbcType="VARCHAR" property="value"/>
        <result column="VALID" jdbcType="VARCHAR" property="valid"/>
        <result column="REMARK" jdbcType="VARCHAR" property="remark"/>
    </resultMap>

    <sql id="item">
        NAME,
        VALUE,
        VALID,
        REMARK
    </sql>

    <select id="queryConfigVariable" resultMap="map">
        SELECT
        <include refid="item"/>
        FROM
            KAOS.CONFIG_VARIABLE
        <where>
            <if test="name != null">
                AND NAME = #{name}
            </if>
            <if test="valid != null">
                AND VALID = #{valid}
            </if>
        </where>
    </select>
</mapper>