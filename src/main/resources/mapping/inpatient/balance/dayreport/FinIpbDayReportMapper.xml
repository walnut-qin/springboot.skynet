<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kaos.his.mapper.inpatient.balance.dayreport.FinIpbDayReportMapper">
    <resultMap id="map" type="com.kaos.his.entity.inpatient.balance.dayreport.FinIpbDayReport">
        <id column="STAT_NO" jdbcType="VARCHAR" property="statNo"/>
        <result column="BEGIN_DATE" jdbcType="VARCHAR" property="beginDate"/>
        <result column="END_DATE" jdbcType="VARCHAR" property="endDate"/>
        <result column="OPER_CODE" jdbcType="VARCHAR" property="rptEmplCode"/>
        <result column="OPER_DATE" jdbcType="VARCHAR" property="rptOperDate"/>
        <result column="CHECK_FLAG" jdbcType="VARCHAR" property="chkFlag"/>
        <result column="CHECK_OPER" jdbcType="VARCHAR" property="chkEmplCode"/>
        <result column="CHECK_DATE" jdbcType="VARCHAR" property="chkDate"/>
    </resultMap>

    <sql id="item">
        STAT_NO,
        BEGIN_DATE,
        END_DATE,
        OPER_CODE,
        OPER_DATE,
        CHECK_FLAG,
        CHECK_OPER,
        CHECK_DATE
    </sql>

    <select id="queryDayReport" resultMap="map">
        SELECT
        <include refid="item"/>
        FROM
            XYHIS.FIN_IPB_DAYREPORT
        <where>
            <if test="statNo != null">
                AND STAT_NO = #{statNo}
            </if>
        </where>
    </select>

    <select id="queryDayReprots" resultMap="map">
        SELECT
        <include refid="item"/>
        FROM
            XYHIS.FIN_IPB_DAYREPORT
        <where>
            <if test="beginDate != null">
                AND OPER_DATE &gt;= #{beginDate}
            </if>
            <if test="endDate != null">
                AND OPER_DATE &lt;= #{endDate}
            </if>
            <if test="deptOwn != null and deptOwn != @com.kaos.his.enums.DeptOwnEnum@All">
                AND (
                    SELECT
                        T2.DEPTOWN
                    FROM
                        DAWN_ORG_EMPL T1
                        LEFT JOIN DAWN_ORG_DEPT T2 ON T2.DEPT_ID = T1.DEPT_ID
                    WHERE
                        T1.VALID_STATE = 1
                        AND T2.VALID_STATE = 1
                        AND T1.EMPL_ID = XYHIS.FIN_IPB_DAYREPORT.OPER_CODE
                ) = #{deptOwn}
            </if>
        </where>
    </select>
</mapper>